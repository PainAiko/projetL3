{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-info">{{ button_label|default('Sauvegarder') }}</button>
    <a role="button" class="btn btn-danger" href="{{ path('categorie_index') }}">Annuler</a>
{{ form_end(form) }}
