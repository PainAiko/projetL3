{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
   <div class="col-md-6 col-xs-12 col-md-offset-3 spacer " style="margin-top : 60px;">
         <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ produit.libelle }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ produit.qte }}</td>
            </tr>
            <tr>
                <th>Pu</th>
                <td>{{ produit.pu }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ produit.idCategorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">back to list</a>

    <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit/_delete_form.html.twig') }}
   </div>
   
{% endblock %}
