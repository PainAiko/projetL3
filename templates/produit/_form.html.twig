{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-info">{{ button_label|default('Sauvegarder') }}</button>
    <a  class="btn btn-info" role="button" href="{{ path('produit_index') }}">Retour</a>
{{ form_end(form) }}
